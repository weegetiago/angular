import { MeusClientesDirective } from './meus-clientes.directive';

describe('MeusClientesDirective', () => {
  it('should create an instance', () => {
    const directive = new MeusClientesDirective();
    expect(directive).toBeTruthy();
  });
});
